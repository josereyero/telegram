services:
  # This is not working because of a service_container bug that prevents
  # services defined in yml from use the ones in the default container services.
  #logger.channel.telegram:
  #  class: Drupal\service_container\Logger\LoggerChannel
  #  factory: ['Drupal\telegram\TelegramLogger', 'create']
  #  factory: '@logger.factory:get'
  #  arguments: ['telegram']
  telegram.logger:
    class: Drupal\telegram\TelegramLogger
    arguments: ['telegram']
  telegram.storage:
    class: Drupal\telegram\DrupalTelegramStorage
  telegram.settings:
    class: Drupal\telegram\TelegramSettings
    arguments: ['telegram_']